# secrets — never commit
.env
.env.local
.env*.local

# runtime data — holds OAuth tokens (google-token.json, zoom-token.json) + bookings
.data/

# deps / build
node_modules/
.next/
dist/
*.log
.DS_Store
