[build]
  command = "npm run build"
  publish = ".next"

# Official Next.js runtime for Netlify (SSG pages + serverless functions).
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
