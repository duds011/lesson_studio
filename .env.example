# ─────────────────────────────────────────────────────────────
# Supabase — student/teacher accounts + portal (NEW)
# Project: lesson-studio (ref pbxyilslgxuxoztixotw)
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://pbxyilslgxuxoztixotw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# Secret — server only. Supabase → Project Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Used only by scripts/seed.mjs to create the teacher login when seeding.
SEED_TEACHER_EMAIL=teacher@example.com
SEED_TEACHER_PASSWORD=change-me

# ─────────────────────────────────────────────────────────────
# Existing integrations (unchanged) — see README for full details:
#   GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET, ZOOM_*, RECALL_API_KEY,
#   OPENAI_API_KEY, KV_REST_API_URL / KV_REST_API_TOKEN, APP_URL
# ─────────────────────────────────────────────────────────────
